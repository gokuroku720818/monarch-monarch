# 모나크 모나크 V171 — 75개 스테이지 원본 음악 매핑 완전 복원 보고서

## 작업 목표
V170까지는 원본 `Data1.dat`의 75개 맵 중 39개만 기본 음악을 확정했고, 나머지 36개는 임의 추정하지 않고 미확인으로 남겨 두었다. V171은 이 36개를 원본 실행 파일의 압축 해제 방식으로 직접 복원해 75개 전부의 기본 음악 ID를 확정한다.

## 확인된 기존 원인
V165의 음악 분석은 `Data1.dat` 내부의 압축 레코드에서 `Ver1.6+` 주변 바이트를 직접 읽는 방식이었다. 일부 레코드는 압축된 바이트열에서도 필요한 값이 우연히 노출되어 39개가 확인됐지만, 나머지 36개는 같은 방식으로 읽을 수 없었다.

원본 `lm_win.exe`를 다시 대조한 결과 실제 게임은 맵 레코드를 그대로 읽지 않는다.

- `0x40902B` 계열: `Data1.dat` 레코드 로드 및 압축 해제 경로
- `0x401000` / `0x4010EC` 계열: 원본 압축 해제 루틴
- `0x407B0D..0x407B2D`: 압축 해제된 맵 헤더를 복사하고 `Ver1.6+` 확인
- `0x407C58..0x407C5E`: 압축 해제된 헤더 `+0x0C`의 32비트 값을 음악 기준값 전역(`0x4C278C`)으로 사용
- `0x436A02` 부근: 이 기준값을 10 단위 곡 그룹으로 처리해 영토 상황에 따른 변주곡(+1)을 결정

따라서 음악값은 압축 전 원본 맵 헤더를 복원한 뒤 읽어야 한다.

## V171에서 복원한 원본 데이터
원본 EXE의 압축 형식을 Python으로 옮겨 `Data1.dat`의 75개 맵 레코드를 모두 압축 해제했다.

- 75/75 레코드 모두 압축 해제 후 `Ver1.6+` 헤더 확인
- 각 맵의 해제 크기: 49,200 bytes
- 헤더 `+0x0C`의 음악 기준값은 모두 `0`, `10`, `20`, `30` 중 하나
- 최종 분포:
  - `LM000`: 36개
  - `LM010`: 13개
  - `LM020`: 13개
  - `LM030`: 13개

V170에서 미확인으로 남아 있던 36개 맵은 모두 원본 데이터상 `LM000`으로 확인됐다.

## 코드 변경 범위
- V170의 이동/전투/AI/경제/세율/건설/장군/지형/승패 로직은 변경하지 않았다.
- 원본 음악 스크립트의 `confirmedStageMusic`을 39개에서 75개로 확장했다.
- 근거 주석과 버전 표기만 갱신했다.
- 경량판은 기존 정책대로 배경음악 자체를 포함하지 않으므로 게임 로직과 버전만 V171로 유지한다.

## 검증
- 신규 원본 데이터 테스트: V170에서 실패, V171에서 통과
  - 원본 `Data1.dat` 75개 압축 해제 결과와 HTML 매핑 75개가 정확히 일치
  - 기존 미확인 대표 맵이 `LM000`으로 복원되는지 확인
- 기존 영토 음악 전환 테스트 5개 통과
- 기존 즉시 음악 전환 주기 테스트 2개 통과
- 원본 음악 임베딩/수동 재생·정지 테스트 통과
- V170과 V171의 게임플레이 스크립트 4개는 버전 문자열을 정규화한 뒤 SHA-256이 동일함을 확인
- V171 전체판의 5개 JavaScript 블록 모두 `node --check` 통과

### 오래된 테스트 1개의 전제 변경
V164 시절 `test_rapid_track_switch_cannot_restore_stale_song`은 0번 스테이지 음악이 미확인이라는 상태에서 `LM000 → LM010`을 내부 API로 직접 재생해 마지막 곡이 유지되는지 검사했다. V171에서는 0번 스테이지의 원본 기본곡이 `LM000`으로 확정되었으므로 게임 틱의 원본 자동 음악 동기화가 `LM010`을 `LM000`으로 되돌리는 것이 정상이다. 따라서 이 테스트는 V171의 원본 스테이지 음악 동기화와 충돌하는 구형 테스트 전제이며, 음악 플레이어 오류로 판정하지 않았다.

## 남아 있는 미검증 범위
- Windows 원본 MIDI 장치와 브라우저 GM 렌더링의 음색/지연은 1:1 동일하지 않다.
- 원본 Windows 실행 파일과 동일 입력·동일 난수로 장시간 동시 실행한 완전 상태 비교는 아직 미완료다.
- 희귀 AI 분기, 일부 세부 UI/렌더링 타이밍은 계속 원작과 대조해야 한다.

V171은 “게임 전체가 원작과 완전히 동일하다”는 의미가 아니라, **75개 스테이지의 기본 음악 ID에 대해서는 원본 EXE의 실제 압축 해제 경로와 원본 Data1 레코드를 기준으로 전부 확정한 버전**이다.
