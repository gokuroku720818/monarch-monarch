# 모나크 모나크 V180 — 명령 중 자동 일시정지·일시정지 표시 동기화

## 기준과 검증 한계

- V179 경량판 Git blob `3f5f16e4bc21154b575bded315c525ba976f2eda` 및 배경음악 포함 전체판 Git blob `0436cc27c47754b0661b0a76f520cfb7601e6f90`의 정확한 바이트에서 생성했습니다.
- 사용자 요청대로 명령 입력 중 게임 시뮬레이션을 멈춥니다. 원작 Windows EXE의 실제 게임 명령 시 정지 여부는 직접 실행하여 확인하지 못했으며, 이 조작을 원작과 일치한다고 단정하지 않습니다.
- 기존 V179의 맵 75개, 원본 제목·장군 마커, 효과음 159개, 이미지 및 BGM 리소스는 변경하지 않았습니다. AI·전투·경제·병력·속도 계산 공식은 변경하지 않았습니다.

## 확인한 원인

V179 엔진에는 별도 임시 상태인 `commandPause`와 `update()`의 시뮬레이션 차단 조건이 이미 존재했습니다. 그러나 부대 선택·목적지 선택·장군 명령 등 모든 명령 진입 경로가 `setCommandPause(false)`를 실행했습니다. 그 결과 명령창이 열려도 게임 시간이 흘렀습니다. 스테이지 초기화도 수동 정지 변수 `paused`는 해제하지만 버튼 텍스트를 `일시정지`로 되돌리지 않아 잘못된 상태를 표시할 수 있었습니다.

## 수정 내용

- 병사 단일 클릭, 다중 선택·Shift 클릭, 장군 명령·목적지·명령창 중에는 `commandPause=true`로 시뮬레이션만 정지합니다. 렌더링과 입력은 계속 동작합니다.
- 명령 확정 또는 부대 선택 완전 취소 시 해제합니다. 명령 실패·목적지 재선택·범위 선택 취소 후 선택이 남았을 때는 정지를 유지합니다.
- 수동 일시정지 `paused`와 명령 정지를 분리해, 수동 정지 중 명령을 끝내도 수동 정지가 풀리지 않습니다.
- 맵 변경 시 수동 일시정지 버튼 글자를 실제 초기 상태에 맞게 되돌립니다.

## 재현·회귀 테스트

- 수정 전 V179 브라우저 테스트에서 명령 중 정지 관련 9개 항목과 스테이지 버튼 표시 1개 항목 실패 확인.
- 수정 후 V180 경량판·전체판: 명령 중 정지, 취소 및 확정 후 진행, 장군 명령, 수동 정지 유지, 맵 변경을 검사하는 실제 Chromium 테스트 23개 항목 통과. 실제 캔버스 클릭→오른쪽 클릭 취소 경로 테스트 통과.
- 원래 배포된 명령 함수들을 추출한 Node 테스트에서 정상/실패 명령, 왕 명령, 수동 정지, 시뮬레이션 차단 통과.
- 기존 다리·울타리 건설 회귀 테스트 양 판 통과.
- 맵 75개 × 100틱을 경량판·전체판 각각 Chromium에서 실행: 검사 범위에서 시간 정지·장군/병사 겹침·브라우저 JS 오류 없음.
- 원본 MAP 75개, 스테이지 제목/장군 마커, WAV 159개 해시 검사 통과. V179과 긴 Base64 리소스 257개(경량)/270개(전체) 동일, 4/5개 JS 블록 문법 검사 통과.
- GitHub Actions: 원본 자산·명령 일시정지·기존 건설·장군 패배·슬롯 재사용·터치 회귀 및 JS 문법 검사 통과. 기본 `index.html`과 V180 보관본은 Git blob `4ae885ab5a823847defd8b86cbeeeab1fe036a11`로 일치합니다.

**미검증**: 원본 Windows EXE와 동일 입력으로 전체 게임 장시간 1:1 비교, 모든 스테이지의 승패·경제·AI 완전 동등성. 웹 기본판은 BGM 제외 경량판이고 별도 ZIP에는 BGM 13곡 전체판이 포함됩니다.
