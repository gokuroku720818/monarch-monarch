# 모나크모나크 V175 — 모바일 앱 전환 이후 선택 복구

## 기준
- 사용자가 제공한 Windows 원작 `lm_win.exe` / `Data1.dat` / `Data1.lib` / WAV 파일에서 추출한 원본 자산 기준값과 V174 실행판.
- 목적은 새로운 게임 규칙이 아니라 원작 방식의 마우스 선택을 모바일로 중계하는 V174 입력 연결의 실제 오류 수정이다.

## 재현과 원인
- V174: 터치 선택 도중 모바일 앱/탭 전환으로 `blur`가 발생하고 `touchend`가 누락되면, 기존 `cancelAbandonedRange()`는 선택 사각형·마우스 누름 상태만 취소하고 `marqueeTouchId`는 남긴다.
- 복귀 후 새 터치를 `touchstart`가 무시하여 부대 두 명 드래그 선택 결과가 0명이 된다.
- 수정 전 Playwright 회귀 테스트 실패와 생산용 터치 핸들러만 실행하는 Node 검사 실패를 각각 확인했다.

## 수정 범위
- V175: 기존 `blur` 이벤트에서 남은 터치 식별자를 `null`로 초기화한다. 원작 스타일의 `mousedown`/`mousemove`/`mouseup` 부대 선택 코드를 재사용한다.
- 이동/전투/AI/경제/세율/건설/장군/지형/승패 시스템, 75개 MAP, 그래픽, 원본 WAV 159개, 전체판 MIDI/OGG 음악은 변경하지 않았다.
- 모바일 터치 처리는 Windows 원작에는 없는 입력 호환 계층이며, 원작에서 확인되지 않은 신규 게임 기능을 추가한 것이 아니다.

## 검증
- TDD: V174에서 손가락 터치 → `blur` → 기존 터치 종료 누락 → 새 손가락 드래그 시 0명 선택으로 실패하는 테스트 작성; V175에서 같은 검사 통과.
- 모바일 Playwright 테스트: V175 경량판 3개, 전체판 3개 통과(포커스 복구, 일반 터치 드래그, 단일 터치 선택).
- 원본 자산 SHA-256 회귀 검사: 두 파일에서 MAP 75개(지형·제목·장군 마커) 및 WAV 159개 원본 바이트 일치.
- JavaScript 구문 검사: 경량판 4개, 전체판 5개 스크립트 블록 통과.
- `tests/test_touch_focus_adapter.js`: 배포될 HTML에서 실제 터치 중계 코드를 추출해 포커스 복귀가 처리되는지 검증한다. V174 실패 → V175 통과.

## 미완료
- GitHub Pages 경량판에는 BGM이 없다. 별도 전체판 HTML에는 기존 13개 음악 파일이 포함된다.
- 원본 Windows EXE와 HTML을 장시간 동일 입력으로 실행하며 AI·전투·경제·승패 결과까지 1:1로 대조한 것은 아니다.
