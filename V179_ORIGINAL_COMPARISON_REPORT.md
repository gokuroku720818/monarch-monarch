# 모나크 모나크 V179 — 울타리 건설 가능 위치와 실제 작업 높이 판정 일치

## 기준 및 수정 범위

- V178 경량판 Git blob `c2ae56bba97734737aac730ad2a0674f9b490bb7` 및 전체판 Git blob `ef276b2161a09d7303fcfde009f19eeb217bddf0`에서 생성.
- 원작 MAP 75개, 제목·장군 마커, WAV 159개, 스프라이트·BGM 포함 내장 Base64 데이터는 그대로 유지.
- AI, 전투력, 경제, 병력 생산, 병력 슬롯, 원본 높이 허용 범위(-2~+3)는 변경하지 않음.

## V178에서 확인한 문제

울타리는 지면 z=2의 윗층 z=3에 설치합니다. V178의 `issueFence()`와 `commandAvailability()`는 경로 길이만으로 작업 위치를 선택하고, 실제 `workFence()`와 실행기는 설치 대상 z=3과 부대 위치의 높이 차이 -2~+3을 검사합니다. 따라서 z=6 위치에서 z=3에 건설하는 불가능한 작업 명령이 수락되며, 반대편의 적합한 z=5 작업 위치가 있어도 먼저 발견한 z=6을 선택할 수 있습니다.

## V179 수정

- `issueFence()`는 최종 울타리 작업 높이로 인접 작업이 가능한 위치만 경로 후보로 허용합니다.
- `commandAvailability()`도 지면+1 신설 울타리, 기존 울타리 하단, 기존 울타리 상단 클릭 시 하단을 가리키는 규칙을 동일하게 반영합니다.
- 유효한 위치가 하나도 없다면 울타리 명령을 활성화하지 않고 이미 실행 중인 부대 명령을 덮어쓰지 않습니다.
- 표시용 버전명을 V179로 변경하고, 다른 게임플레이 코드는 변경하지 않았습니다.

## 검증 결과 및 한계

- 실패 우선 테스트: V178의 실제 `issueFence()` 코드에서 z=6을 먼저 고르는 실패를 재현했습니다.
- 수정 후 Node 테스트: 유효 z=5 위치 우선 선택, 전부 불가할 때 명령 거부·기존 상태 보존, 기존 울타리 하단 및 상단 클릭 분기, 경량판·전체판 통과.
- 이전 다리 높이 회귀 테스트 통과. 원작 MAP 75개, 제목·장군 마커, WAV 159개 해시 일치, 내장 Base64 경량판 257개·전체판 270개 모두 V178과 동일, 경량판 JS 4블록·전체판 5블록 문법 검사 통과.
- Chromium 경량판과 전체판 각각 75개 맵 × 100틱 테스트: 게임 시간 정지·장군/병력 동일 칸 초기 배치·JavaScript 오류 없음(해당 검사 범위).
- GitHub Actions 배포 파이프라인: 기존 원본 데이터 검사·울타리·다리·장군 패배·병력 슬롯 재사용·터치 포커스 회귀 검사 통과. `index.html`과 V179 보관본 Git blob 일치 검증.
- 원본 Windows EXE와 수시간의 동일 입력 대조 및 원본 게임 전체의 행동 1:1 동등성은 **미검증**입니다. 위 테스트는 해당 오류의 수정과 제한된 회귀 범위만 뒷받침합니다.

GitHub Pages 기본판은 BGM 없는 경량판입니다. BGM 13곡 포함 전체판은 ZIP에 별도로 포함합니다.
