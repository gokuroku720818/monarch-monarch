# 모나크 모나크 V178 — 높은 지형의 다리 건설 명령·실행 불일치 수정

## 기준과 적용 범위
- 사용자 제공 `monarch_v177_complete.zip`의 V177 경량판 및 BGM 포함 전체판을 각각 기준으로 수정했습니다.
- GitHub Pages 기본판은 `index.html` = `monarch_v178_lite.html`이며, 기존 v177은 별도 보관합니다.
- 원본 자료의 지형·맵 제목·장군 위치·효과음·기존 그래픽·BGM 데이터는 변경하지 않았습니다.
- 이번 수정은 다리 작업의 높이 판정 두 지점과 버전 표기만 바꿉니다. AI·전투·경제·다른 건설 수치를 수정하지 않았습니다.

## 확인한 오류
맵 `M_000`, 수면 `(x=16,y=7,z=2)`, 원본 타일 `87`, 인접 작업 부대 위치 `(x=15,y=7,z=5)`를 사용해 재현했습니다.

1. 명령 UI의 `bridgeOrderStand()`는 완성될 다리 상판 `z=3`과 부대의 높이 차이 `-2`를 검사해 명령을 수락합니다.
2. V177 행동 분기와 `workBridge()`는 물 타일 `z=2`와 높이 차이 `-3`만 검사하여 작업을 호출하지 않거나 거부합니다. 실제 브라우저 테스트에서 명령은 수락됐지만 다리가 생기지 않았습니다.
3. V178은 두 지점 모두 물 타일에 대해서만 `z+1`(상판)을 검사하고, 기존 완성·손상된 다리는 해당 타일 자체의 높이를 유지합니다.

이는 HTML 내부의 명령/행동 판정을 일치시키는 검증된 수정입니다. 원본 Windows EXE의 모든 다리 건설 장면을 1:1 실행 비교했다는 주장은 아닙니다.

## 수정 전 실패 → 수정 후 통과
- Node 테스트: 높은 지형에서 명령 수락 → `workBridge()` 거부 (V177 재현), V178에서는 정상적으로 `z=3`에 가설 다리(80) 생성, 비용 20 지출.
- 추가 경계 검사: 상판도 사거리 밖인 높이 차이 `-3`은 여전히 작업 거부, 자금 무소모. 일반적인 인접 수면 작업도 통과.
- Chromium 실제 실행: 높이 5 부대에서 명령 입력 후 **5틱 이내에 상판 타일 59, 내구도 100**으로 건설 완료(경량판/전체판 모두). 전투 중 부대가 먼저 죽는 변수를 배제하려고 테스트에서는 부대 병력을 5,000으로 설정했습니다.

## 다른 요소 회귀 검사
- 맵 75개를 각각 100틱씩 실행: 잘못된 슬롯 수·시간 정지·브라우저 JS 오류 없음(검사 범위 한정).
- 양 판의 맵 75개 데이터 및 내장 이미지/음성 Base64 23개가 해당 V177 기준판과 바이트 단위로 일치.
- JavaScript 문법 검사: 경량판 4블록 / 전체판 5블록 통과.
- GitHub Actions의 원본 MAP 75개, 제목·장군 마커, WAV 159개 데이터 검증 및 이전 회귀 테스트 통과.

## 배포·미검증 항목
- GitHub 기본 파일 `index.html`은 **V178 경량판**으로 교체하고, 이전 V177 파일은 유지합니다. BGM 포함 전체판은 제공된 ZIP에만 포함했습니다.
- 원작 Windows EXE와 게임 전체의 장시간 AI·경제·전투·승패를 같은 조작으로 장시간 1:1 대조하지 않았습니다. 완전 복원본으로 표시하지 않습니다.
