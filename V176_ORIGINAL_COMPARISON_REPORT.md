# 모나크모나크 V176 — 원작 64슬롯 부대 교체 시 이전 명령 UI 정리

## 기준 및 목표
- 기준 실행판: GitHub의 V175 `index.html` (Git blob `88b953b3ffea7c00466d5db4a5820ad61590e431`).
- 원작 기준: 사용자가 제공한 Windows `lm_win.exe`와 `Data1.lib`·`Data1.dat` 및 원본 WAV에서 만든 고정 자산 검사값.
- 원작의 64개 부대 슬롯은 재활용되지만, 이전 부대의 선택·명령 상태가 새 부대로 이전되어서는 안 된다. 원작의 전투/경제 알고리즘은 이번 패치의 변경 대상이 아니다.

## V175에서 확인한 차이
브라우저에서 M_000의 병사를 선택하고 지형 명령 메뉴를 연 후, 해당 병사가 제거되고 같은 슬롯에 새 병사가 생성되는 상황을 재현했다. `addUnit()`은 `selected.delete(slot)` 후 `__MONARCH_RECONCILE_SELECTION__()`을 호출하지만, UI의 `reconcileSelection()`은 선택 집합을 **이미 비운 상태**에서는 내부 변경이 없다고 판단해 일찍 반환했다. 결과는 선택 0명인데 명령 메뉴가 열리고 `uiPhase=action`, 이전 `pendingCell`이 남는 오류였다.

## V176 수정
- 부대 슬롯 재사용으로 선택을 삭제한 경우 `__MONARCH_RECONCILE_SELECTION__(true)`로 강제 UI 정리를 요청한다.
- `reconcileSelection(force=false)`는 기존 일반 프레임에서는 변경 감지를 유지하고, 강제 호출 시 명령 메뉴/이전 대상/입력 단계/명령 일시정지 상태를 정리한다.
- 게임의 부대 생성 위치, 64슬롯 제한, 이동·전투·AI·경제·지형·승패 계산과 원본 그래픽·맵·오디오 바이트는 변경하지 않았다.

## 검증 결과
- 재현 테스트: V175에서 선택 0명인데 메뉴가 열려 있어 실패, V176 GitHub 동일 바이트의 재현 테스트 통과. 배경음악 포함 전체판에서도 통과.
- CI에 추가한 `tests/test_recycled_slot_cleanup.js`는 배포 HTML에서 실제 조정 함수를 추출해 실행하고 슬롯 교체 호출자까지 검사한다. V175에서 실패, V176에서 통과한다.
- 원본 자산 SHA-256 검사: MAP 75개(지형, 스테이지 명칭, 장군 마커)와 원본 WAV 159개 모두 일치. 경량판·전체판 모두 검사했다.
- JavaScript 구문 검사: 경량판 4개, 전체판 5개 블록 통과.
- 기존 슬롯 제한·생산·합류 관련 선택한 테스트: 12개 및 하위 검사 4개 통과.
- 스테이지 75개 로딩, 120회 진행 스모크 검사 각 통과. 기존 모바일 터치 포커스 복구 검사도 통과.

## 남은 차이와 한계
GitHub Pages는 배경음악을 제외한 경량판이다. 전체판 단일 HTML에는 기존 13곡 음악 데이터가 포함된다. 원본 Windows 프로그램과 AI·전투·경제·승패를 동일 조작으로 장시간 직접 1:1 실행 비교한 것은 아니므로 게임 전체의 원작 동일성은 아직 입증되지 않았다.
