# 모나크 모나크 V181 — 작업 명령의 지형 높이·선택 위치 일치

## 기준 및 변경 범위

- V180 경량판 Git blob `4ae885ab5a823847defd8b86cbeeeab1fe036a11`, BGM 포함 전체판 Git blob `f89933ce48f5c0fd379231082d0d0e87e4129b56`를 기준으로 **일치하는 입력만 수정**했습니다.
- 명령 UI의 `commandAvailability()`와 `issueSpecificWork()`만 기능 수정하고 버전 표시만 V181로 변경했습니다. 원본 MAP·스프라이트·WAV·BGM 자산과 게임의 AI·전투·경제·생산·속도 수식은 변경하지 않았습니다.

## 수정 전 재현 (V180)

기존 원작 대조 코드에서 `originalActionAdjacent()`는 작업 위치가 대상의 동서남북 한 칸이면서 높이 차이가 **대상 − 부대 = -2..+3**이어야 한다고 판정합니다. 그런데 건물 파괴·다리 파괴·울타리 파괴·숲/식생 제거·구덩이 봉쇄 명령은 `bestAdjacentRoute()`에서 **경로만** 검사해, 작업 불가능한 높은 둑을 먼저 고릅니다.

테스트: 타깃 z=2, 왼쪽 작업 둑 z=5(높이 차이 -3, 불가), 오른쪽 둑 z=4(차이 -2, 가능). V180은 왼쪽 x=15.5를 선택해 실제 작업 분기에 진입하지 못합니다. 양쪽 둑 모두 사거리 밖이어도 메뉴가 명령을 수락합니다. 기존 울타리 상단 타일 40/42/44/46은 실제로 하단 코어 41/43/45/47을 작업하므로 하단 높이를 따로 적용해야 합니다.

## V181 수정

- 모든 위 작업 명령의 메뉴 가용성과 실행 시 작업 위치를 실제 작업 대상 높이로 필터링합니다. 거리·길찾기 공식이나 높이 허용 범위 자체를 변경하지 않습니다.
- 울타리 상단을 클릭한 경우 아래 한 칸에 짝을 이루는 코어가 있는지 확인하고, 코어 높이로 작업 가능성을 판정하고 명령 목표를 정규화합니다.
- 가능한 작업 위치가 없으면 명령 버튼을 비활성화하고 기존 유닛 명령을 덮어쓰지 않습니다. 가능한 위치가 하나 이상이면 경로가 짧은 **실제로 작업 가능한** 위치를 선택합니다.

## 검증

- 실패 우선 Node 테스트: 배포 V180 실제 명령 함수에서 숲 제거 시 불가능한 x=15.5를 선택하는 실패 재현.
- 수정 후 경량판·전체판에서 7가지 유형(숲, 식생, 생산기지, 다리, 울타리 하단·상단, 구덩이) × 유효/불가 지형과 기존 명령 보존 회귀 검사 통과.
- 기존 다리·울타리 명령 검사, 명령 중 일시정지 Node 및 Chromium 테스트(각 버전 23개 점검), 실제 클릭/오른쪽 클릭 취소 테스트 통과.
- 경량판·전체판 각각 75개 맵을 100틱 실행: 해당 테스트 범위 내 시간 정지, 초기 장군·병사 겹침, JavaScript 실행 오류 없음.
- 원본 MAP 75개, 제목·장군 마커, WAV 159개 해시 일치. 이전판 대비 내장 Base64 경량판 257개 / 전체판 270개 해시 동일. JS 4/5블록 구문 검사 통과.

## 남은 제한

원작 Windows EXE의 모든 동작과 게임 전체를 장시간 동일 조작으로 1:1 대조한 것은 아닙니다. 이번 수정은 확인된 작업 선택 불일치를 기존 엔진의 원작 유래 작업 조건과 일치시킨 범위에 한정합니다. GitHub Pages는 BGM 제외 경량판이며 전체 HTML은 ZIP 파일에 포함됩니다.
