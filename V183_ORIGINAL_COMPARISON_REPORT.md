# 모나크모나크 V183: 완성 다리 명령과 실제 작업 판정 일치

## 기준과 변경 범위

- V182 경량판 Git blob `f655016b8c1c3b111311c81b53b4d287980affd0`, BGM 포함 전체판 `415f3c484292173a00d591fbaf5d356f1ee39049`에서 생성.
- `bridgeOrderStand()`의 기존 완성 다리 판정만 기능 변경하고 표시 버전명을 V183으로 갱신.
- 맵, 스테이지 정보, WAV, 내장 이미지/음악 데이터, AI·전투·경제 계산, 명령 중 일시정지 코드는 변경하지 않음.

## V182에서 재현한 오류

`commandAvailability()`는 완성 다리 타일(58..68)의 DF가 100이면 다리 건설/수리 메뉴를 비활성화한다. 실제 `workBridge()`도 `missing<=0`이면 작업을 거절한다. 그러나 `issueBridge()`가 사용하는 `bridgeOrderStand()`는 DF를 확인하지 않아, DF100 완성 다리에도 직접 명령을 발행할 수 있었다. 즉 메뉴와 실제 작업 규칙은 거절하는데 명령 발행 계층만 허용하는 불일치였다.

실패 우선 검증: `tests/test_bridge_command_validity.js`를 V182 경량판에 실행했을 때 `Direct order must reject completed bridge DF100`에서 실제값 `true`, 기대값 `false`로 실패했다.

## V183 수정

- 완성 다리 타일(58..68)의 DF를 `bridgeOrderStand()`에서도 확인.
- DF100 이상이면 명령 발행 자체를 거부하고 기존 병사 명령을 보존.
- DF100 미만 완성 다리는 수리 명령 유지.
- 손상 다리 타일(69..79), 공사 발판(80), 물 위 신규 다리(83..110)는 기존 규칙 유지.
- 자원 비용, 작업량, 병력 소모, 다리 모양 갱신, 작업 가능 높이 계산은 수정하지 않음.

## 검증

- V182 실패 → V183 경량판·전체판 Node 회귀 테스트 통과.
- Chromium 실제 명령 UI에서 경량판·전체판 모두 DF100 다리 버튼 비활성 및 직접 명령 거부, DF70 다리 수리 명령 허용 확인. 두 판 모두 페이지 오류 0건.
- GitHub Actions run `36015519164`에서 V183 생성, 원본 자원 검사, 새 다리 명령 테스트, 기존 울타리 명령/작업 높이/명령 일시정지/울타리 높이/다리 높이/장군 패배/병력 슬롯/터치 포커스 회귀 및 JavaScript 문법 검사를 모두 통과한 뒤 `index.html`과 `monarch_v183_lite.html`을 게시.
- 게시된 `index.html` Git blob `d366a1c67bfb97e495cb80be11cb8312390da85d`는 로컬 V183 LITE와 동일. BGM 포함 전체판 Git blob은 `f3fd01924c227424521664232a53249560d746c2`.
- V182→V183 비교에서 `STAGES` 및 `SOUND_DATA` 데이터가 바이트 동일하고, 검사한 내장 Base64 데이터도 동일.

## 한계

원작 Windows EXE와 동일 입력을 넣어 AI·전투·경제·승패·명령 UI를 장시간 1:1 비교한 검증은 아직 완료되지 않았다. V183은 현재 복원 엔진 내부의 원작 유래 `workBridge()` 규칙과 명령 메뉴/발행 동작을 일치시킨 확인된 오류 수정이며, 원작 전체와 완전히 동일하다는 의미는 아니다.
