# 모나크모나크 V173 — 원작 파일 직접 대조 및 WAV 호환성 수정

## 확인한 원본 근거

- 사용자가 제공한 원작 압축파일의 `lm_win.exe`, `Data1.lib`, `Data1.dat` 및 원본 WAV 파일을 대조했다. 원본 Windows 파일 자체는 GitHub에 게시하지 않았다.
- 원본 압축 해제 방식으로 75개 스테이지의 49,152바이트 맵 데이터를 복원하고 HTML 데이터와 비교했다. 75개 모두 동일하다.
- 원본 맵 75개의 실제 스테이지 제목과 성/장군 위치 마커가 HTML과 동일하다. 전체 75맵 초기화에서 병사가 성안에 생성되는 오류를 발견하지 않았다.
- 원본 WAV 효과음 159개의 파일명과 오디오 바이트 모두 일치한다. 원본 파일명에는 대문자 `LM0000.WAV`와 소문자 `lm0200.wav`가 공존한다.

## V173에서 수정한 차이

원본 Windows의 파일명 검색과 달리 V172 `audio(name)`의 `SOUND_DATA[name]`는 대소문자를 구별한다. `LM0200.WAV`로 호출해 `lm0200.wav`를 찾지 못하고, 소문자 `lm0000.wav`로 대문자 `LM0000.WAV`를 찾지 못하는 문제를 확인했다. V173에서는 원래 이름으로 먼저 조회하고, 없으면 소문자/대문자 이름으로 순서대로 조회한다. 새로운 게임 기능을 추가하거나 원본 자산/맵/AI/경제/전투를 임의로 변경하지 않았다.

## 검증

- V172에서 실패한 WAV 대소문자 브라우저 테스트는 V173 전체판 및 경량판 모두 통과.
- 원본 자산 SHA-256 기반 회귀 테스트: 75맵, 원본 제목/장군 위치 및 WAV 159개 통과.
- 원작 관련 기존 테스트 9개 및 하위 검증 251개 통과.
- 인라인 JavaScript 구문 검사: V173 경량판 4개, 전체판 5개 스크립트 통과.
- GitHub Actions는 새 HTML 두 개의 일치 및 원본 맵/WAV 무변경 검사 후 게임을 커밋한다.

## 미검증 및 차이

GitHub Pages는 배경음악을 뺀 경량판(V173 LITE)이며 원본 WAV 효과음은 포함된다. 원본 음악을 포함한 V173 전체판은 대화 내 HTML/ZIP 파일로만 제공된다. 원작 Windows EXE와 장시간 AI, 경제, 명령/전투를 같은 입력으로 1:1 실행해 완전 동일성을 검증하지는 않았으므로 원본과 완전히 같다고 주장하지 않는다.
