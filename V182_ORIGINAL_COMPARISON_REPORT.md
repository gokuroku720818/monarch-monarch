# 모나크 모나크 V182: 울타리 명령 활성화 조건과 실제 작업 판정 일치

## 기준과 변경 범위

- V181 경량판 Git blob `eb333fac361ba9da495fdc86f3fe86a7ae4f0b67`, BGM 포함 전체판 `bdfc3ea216e05fb0404b3d67f4d0221869c019ae`에서 생성.
- `commandAvailability()`와 `issueFence()`만 기능 변경; 표시 버전명을 V182로 수정.
- 원본 MAP, 제목/왕 마커, 스프라이트, WAV, BGM Base64, AI·전투·경제·생산 계산 및 명령 중 일시정지 코드는 변경하지 않음.

## V181에서 재현한 오류

게임의 `workFence()`는 이미 내구도 200인 울타리는 `missing<=0` 조건으로 작업을 거절한다. 하지만 명령 메뉴와 `issueFence()`는 완료된 울타리에도 수리/건설 명령을 승인한다. 또한 신설 울타리의 실제 작업 코드는 코어 z, 상단 z+1이 모두 비어 있고 코어 z<47이어야 하지만, 명령 메뉴에서는 이 검사를 하지 않아 실행 불가능한 명령이 활성화되었다. 페어가 없는 울타리 상단도 작업 대상으로 잘못 처리될 수 있었다.

실패 먼저 확인: `tests/test_fence_command_validity.js`를 V181에 실행하여 `Menu incorrectly enables complete fence core DF200` 실패 재현.

## V182 수정

- 실제 코어 내구도가 200에 도달했다면 울타리 건설 메뉴 비활성화 및 직접 명령 거부.
- 손상된 코어(내구도 200 미만)는 기존처럼 수리 명령 유지; 상단 클릭은 매칭되는 하단 코어로 정규화.
- 새 울타리는 실제 작업 코드처럼 두 층 공간과 z<47을 확인한 뒤에만 메뉴를 활성화.
- 하단 코어가 없는 고립된 상단 타일은 건설/수리 명령을 거부.
- 부적합한 명령을 거부할 때 기존 병사 행동을 변경하지 않음. 실제 작업의 내구도, 비용, 거리, 높이 계산은 수정하지 않음.

## 검증

- V181 실패 → V182 경량판·전체판 Node 회귀 통과: 완성·손상 코어, 상단 클릭, 높이 상한, 코어/상단 점유, 고립 상단, 기존 명령 보존, 정상 신설 건설.
- 기존 울타리 작업 위치, 다리 상판, 다른 작업 7종의 지형 높이, 명령 일시정지 테스트 모두 경량판·전체판 통과.
- 실제 Chromium에서 완성 울타리의 명령 버튼 비활성화 → 내구도 120일 때 활성화 → 수리 명령 코어 높이 설정 확인. 각 판에서 오류 0건.
- Chromium 명령 일시정지 상태 테스트 각 판 23개 검사 및 실제 클릭/우클릭 취소 테스트 통과.
- 경량판·전체판에서 각각 맵 75개를 로드하고 각 10틱 진행: 해당 범위 내 장군/병사 초기 중복·게임 진행 정지·JavaScript 오류 없음. 75맵×100틱 장시간 테스트는 시간 초과되어 V182에 대해서는 **완료하지 못함**.
- 맵 75개와 왕 마커/제목, WAV 159개의 원본 해시 일치. V181 대비 경량판 내장 Base64 257개와 전체판 270개가 모두 바이트 동일. JS 4/5블록 문법 검사 통과.

## 한계

원작 Windows EXE와 동일 입력을 넣어 AI·전투·경제·승패·명령 UI를 장시간 1:1 비교한 검증은 완료되지 않았다. V182는 현재 HTML 엔진에 기록된 원작 유래 `workFence()` 조건과 명령 메뉴/발행 동작을 일치시킨 **확인된 오류 수정**이지 원작 전체의 완전한 복원을 입증하지 않는다. GitHub Pages 경량판에는 BGM이 없고 전체판은 별도 ZIP으로 제공한다.
